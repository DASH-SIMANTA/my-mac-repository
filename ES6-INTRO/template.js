const first = 'simanta'
const last = 'dash'
const greet = 'potas potas';

const name = first +' ' + " " + greet;
console.log(name);

const a = 10;
const b = 20;
const result = 'the sum of '+ a + ' and ' + b + ' is ' + (a + b);
console.log(result);
const numbers = [24,23,45,43,21,11];
const student = {name: 'simanta dash', age: 30}  

const math = `the sum of ${a} and ${b} is ${a+b}`
const studentDetails = `the sum of ${numbers[0]} and ${student.age} is ${a+b}`
console.log(math);
console.log(studentDetails);

const email  = 'hi jhon \n' +
'cena cena lage \n ' +
'bosonto ashe geche';

console.log(email);

const challenge = `ekhon february mash 
sobai prem kore 
ami code kori`
console.log(challenge);


console.log(studentDetails);

